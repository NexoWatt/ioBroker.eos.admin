# NexoWatt EOS Admin v7.9.64

This hardening release synchronizes source and deployed branding assets, updates all cache keys, simplifies the hidden native drawer header, and replaces old heavyweight ObjectBrowser/adapter-react copies with compatibility shims to the single active v61 runtime. Native ioBroker datapoint semantics are unchanged.
