# NexoWatt EOS Admin v7.9.81

This release fixes the v7.9.80 prepublish validation failure while preserving the modern navigation icon design.

- React/ObjectBrowser runtime remains v79.
- Branding cache version is declared separately as v81.
- All package, lockfile, io-package and src-admin versions are synchronized to 7.9.81.
- Validation now catches lockfile and top-level io-package version drift.
