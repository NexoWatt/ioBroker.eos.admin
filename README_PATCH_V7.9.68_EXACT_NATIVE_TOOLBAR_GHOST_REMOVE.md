# v7.9.68 – Exact native toolbar ghost removal

This patch removes only the small native desktop toolbar pill made from the MenuIcon and compact `/#easy` identity block. The primary NexoWatt EOS / Energy Operation System logo remains unchanged. Both source and compiled runtime are patched, with a fresh v68 entry for cache safety.
