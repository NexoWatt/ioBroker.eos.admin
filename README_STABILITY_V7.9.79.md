# NexoWatt EOS Admin v7.9.79

Direct-control reliability release. Boolean buttons with `common.def=false` now emit the active trigger, binary switches use a fresh state before toggling, encoded booleans/numbers are compared typgerecht, button states are not optimistically latched, and the complete active frontend graph has moved to v79.
