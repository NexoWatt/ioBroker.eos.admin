# v7.9.67 – Primary EOS logo restore / native ghost-tab removal

- Restores the large NexoWatt EOS / Energy Operation System header card.
- Removes only the short native NexoWatt button with arrow in the top-left corner.
- Uses a fresh v67 application module graph to prevent cached bootstrap-v61 code.
