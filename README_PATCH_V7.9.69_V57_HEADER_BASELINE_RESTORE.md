# v7.9.69 – v57 header baseline restore

The desktop EOS shell header behavior is restored to the known-good v7.9.57 implementation.
Later v63-v68 top-left header experiments were removed. Module, instance, datapoint and backend fixes remain.
A narrowly bounded geometry fallback hides only a small native NexoWatt tab in the top-left corner and explicitly excludes the large EOS primary brand.
